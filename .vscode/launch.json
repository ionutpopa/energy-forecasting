{
    "version": "1.0.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Forecasting.js",
            "runtimeArgs": [ "-r", "ts-node/register" ],
            "args": [
                "${workspaceFolder}/src/index.ts",
                // "debug",
                // "drop-all",
                // "parse-all-data",
                "build-and-train",
                "consumption",
                // "population-growth",
                // "generation"
            ],
            "cwd": "${workspaceFolder}",
            "outFiles": [
                "${workspaceFolder}/**/*.js"
            ],
            "env": {
                "MAX_RETRIES": "10",
            }
        }
    ]
}